---
{% raw %}
# Users
remote_user_name: vagrant
remote_user_group: vagrant

# Dirs
local_home: "{{ lookup('env','HOME') }}"
local_user: "{{ lookup('env', 'USER') }}"

# Other vars
default_locale: es_AR.UTF-8

# Bootstrap
bootstrap_upgrade_system: false

{% endraw %}
